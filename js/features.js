/** Taxi Xanh - Features (minimal stubs for compatibility) */
const Features = {
  trackQuest() {},
  onPlant() {},
  onHarvest() {},
  onLogin() {},
  checkAchievements() {}
};
if (typeof window !== 'undefined') window.Features = Features;
